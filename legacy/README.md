# Legacy stack

This folder holds the **retired Flutter + Firebase** implementation. It is kept for reference only.

| Path | Contents |
|------|----------|
| `flutter/` | Flutter mobile app (`lib/`, `android/`, `ios/`, `test/`) |
| `firebase/` | Firestore rules, indexes, Cloud Functions |

Active development uses:

- `web/` — Vite + React PWA
- `supabase/` — Postgres + Auth + Edge Functions

See the root `README.md` for current setup.
