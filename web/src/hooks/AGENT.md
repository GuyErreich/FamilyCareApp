# Hooks Folder Agent Notes

Reusable hooks by domain: `auth/`, `family/`, `shifts/`, `notifications/`.

Do not bury reusable hooks inside component folders.

Skills: `code/web/libs/react`, `code/languages/nodejs`.
