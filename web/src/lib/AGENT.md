# Lib Folder Agent Notes

Firebase-free: Supabase client, database types, date helpers, constants.

- `supabase.ts` — typed client
- `database.types.ts` — Postgres row types
- `dates.ts` — calendar/planner time helpers

Skills: `project/platform/supabase`, `code/quality/security` for env vars (`VITE_*` only).
